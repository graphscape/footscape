
curl -XGET 'http://localhost:9200/node-index/user-snapshot/_search?pretty=true' -d '{
    "sort":[
    	{"timestamp_":{"order":"desc"}}
    ],
    "query": {
        "match_all" : { }
    }
}
'

curl -XGET 'http://localhost:9200/node-index/cooper-request/_search?pretty=true' -d '{
    "sort":[
    	{"timestamp_":{"order":"desc"}}
    ],
    "query": {
        "match_all" : { }
    }
}
'
curl -XGET 'http://localhost:9200/node-index/activity/_search?pretty=true' -d '{
    "sort":[
    	{"timestamp_":{"order":"desc"}}
    ],
    "query": {
        "match_all" : { }
    }
}
'
