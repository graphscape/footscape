curl -XGET 'http://localhost:9200/node-index/user-activity/_search' -d '{
    "query": {
        "term":{ "email" : "user1@domain1.com" }
    }
}'

curl -XGET 'http://localhost:9200/node-index/user-activity/_search' -d '{
    "query": {
        "match_all" : { }
    }
}
'

curl -XGET 'http://localhost:9200/node-index/account/_search' -d '{
    "query": {
        "match_all" : { }
    }
}
'

